{
  "name": "hero-cycles-pricing-root",
  "version": "1.0.0",
  "description": "Root workspace config for Hero Cycles Pricing Engine",
  "scripts": {
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "start": "node start-all.js"
  }
}
